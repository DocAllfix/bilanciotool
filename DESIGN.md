# DESIGN.md — registro token (da compilare in Fase 3)

Questo file diventa vincolante in Fase 3 (design foundation con skill `impeccable` + `ui-ux-pro-max` + `dataviz`).

Direzione già decisa col committente ("Corporate Tech", distinta dalla famiglia Ambra di Evalis):
- Base fredda: near-white/grigio perla, testo blu-notte.
- Accento: verde petrolio/foresta scuro. Mai verdi brillanti da greenwashing.
- Tipografia: sans grottesca con **numeri tabellari** per l'app; serif editoriale SOLO per il documento finale.
- Dark mode di serie per l'app.
- Due registri: app densa e silenziosa / documento editoriale ricco.

I token vivranno in `src/app/globals.css` (`@theme`, Tailwind v4). Nessun colore hardcoded fuori dai token.
