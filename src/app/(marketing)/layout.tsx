// Il font display (Bricolage Grotesque) è caricato nel root layout:
// la landing usa le stesse utility font-display del resto del sistema.
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
